const doorRatio = 80 / 200;
const DISPOINT_RATIO = 20;

const COLOR_WIDTH = 24;
const FREE_COLOR = true;
//const COLOR_WIDTH = 12;
//const FREE_COLOR = false;

const FR = 60;

const COLORS = [].concat(
  //SCHEMES.blue,
  SCHEMES.teal,
  SCHEMES.green,
  SCHEMES.lime,
  SCHEMES.skyblue,
  SCHEMES.cyan,
  SCHEMES.red,
  //SCHEMES.orange,
  //SCHEMES.yellow,
);